__pycache__/
*.pyc
.venv/
.uv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.DS_Store

# Frozen/live data caches — event-day artifacts, not committed as source
data/raw/
data/frozen/
data/live_cache.json
data/news_cache.json
*.tif
*.tiff
# osmnx's HTTP response cache (osm_features.py) — .gitignore has no trailing-comment syntax,
# so this must be its own line or "cache/" silently stops matching
cache/

# Secrets (API keys go in Streamlit Cloud → App settings → Secrets)
.streamlit/secrets.toml
.env
*.key
service-account*.json

# Office lock files (LibreOffice leaves these behind)
.~lock.*#
